import 'package:flutter/material.dart';

class AiDoctorPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('AI 약사')),
      body: Center(child: Text('AI 약사 페이지 내용')),
    );
  }
}
